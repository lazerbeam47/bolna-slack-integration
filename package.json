{
  "name": "bolna-slack-alert",
  "version": "1.0.0",
  "description": "Webhook server that sends a Slack alert when a Bolna call ends",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
